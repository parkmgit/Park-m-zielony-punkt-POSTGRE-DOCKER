# PostgreSQL Database Configuration
# Skopiuj ten plik do .env.local i uzupełnij danymi swojej bazy PostgreSQL

# Opcja 1: Użyj DATABASE_URL (preferowana dla serwisów jak Render, Railway, Neon, Supabase)
DATABASE_URL=postgresql://user:password@host:5432/database_name

# Opcja 2: Użyj osobnych zmiennych (jeśli nie masz DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=park_m_trees

# SSL (ustaw na 'true' dla połączeń zdalnych, 'false' dla localhost)
DB_SSL=false

# Node Environment
NODE_ENV=production
